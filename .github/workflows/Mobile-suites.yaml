---
name: Mobile suites
on:
  push:
    branches: ["dev"]

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: "8"
          distribution: adopt

      - name: Installing porject dependencies
        run: | 
          mvn clean test -Dsuite=mobileSuites/${{ secrets.Suite_Name }}